spring:
  application:
    name: ai-coding
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai-coding
    username: root
    password: dbuserdbuser

server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.chihiro.aicoding.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

